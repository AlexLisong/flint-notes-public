import {useState} from 'react';
import {ArrowRight,Flame,LockKeyhole,FolderSync,Globe2} from 'lucide-react';
import type {Session} from '../../shared/types';
import {post} from '../api';
export function Auth({onSession,invite}:{onSession:(s:Session)=>void;invite?:string}){const [register,setRegister]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');return <main className="auth-page"><div className="auth-story"><a className="brand"><Flame size={30}/><span>flint</span></a><div className="auth-copy"><div className="eyebrow">A HOME FOR YOUR THINKING</div><h1>Small sparks.<br/>Lasting ideas.</h1><p>Your notes, connected. A quiet space to write, explore, and make something of what you know.</p><div className="auth-features"><span><LockKeyhole size={18}/> Private by design</span><span><FolderSync size={18}/> In sync, everywhere</span><span><Globe2 size={18}/> Publish on your terms</span></div></div><div className="auth-footer">Plain Markdown. Encrypted vaults. Entirely yours.</div><div className="auth-art"><i/><i/><i/><i/><i/></div></div><section className="auth-form-wrap"><form className="auth-form" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const data=Object.fromEntries(new FormData(e.currentTarget));try{onSession(await post('/auth/'+(register?'register':'login'),{...data,invite}));}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}><span className="eyebrow">YOUR PERSONAL WORKSPACE</span><h2>{register?'Start your next chapter':'Welcome back.'}</h2><p className="muted">{register?'Create an account to open your own space.':'Sign in to pick up where your thoughts left off.'}</p>{register&&<label>Your name<input name="name" required autoComplete="name" placeholder="Alex"/></label>}<label>Email address<input name="email" type="email" autoComplete="email" required placeholder="you@example.com"/></label><label>Password<input name="password" type="password" minLength={12} required autoComplete={register?'new-password':'current-password'} placeholder="At least 12 characters"/></label>{register&&!invite&&<label>Registration code<input name="code" required placeholder="Code from your server administrator"/></label>}{error&&<p className="error" role="alert">{error}</p>}<button className="primary full" disabled={busy}>{busy?'One moment…':register?'Create account':'Sign in'}<ArrowRight size={18}/></button><p className="auth-switch">{register?'Already have an account?':'New to Flint?'} <button type="button" className="text-button" onClick={()=>{setRegister(!register);setError('');}}>{register?'Sign in':'Create account'}</button></p><p className="fine muted">Your account password signs you in. Your separate vault phrase decrypts your notes.</p></form></section></main>;}
